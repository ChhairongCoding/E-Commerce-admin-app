import 'package:flutter/material.dart';

class ToggleMode extends StatelessWidget {
  const ToggleMode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("dflaskjfskalj"));
  }
}
